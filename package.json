{
  "name": "syncthink",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter @syncthink/signaling dev\" \"pnpm --filter web dev\"",
    "dev:web": "pnpm --filter web dev",
    "dev:signaling": "pnpm --filter @syncthink/signaling dev",
    "typecheck": "pnpm --filter web typecheck",
    "build": "pnpm --filter web build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
